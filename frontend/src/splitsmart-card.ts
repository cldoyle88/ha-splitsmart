// Splitsmart Lovelace custom card — M2 entry point.
// The hello-world render lands in step 5; this file exists now so the
// Rollup build has an input to resolve while CI is wired up.

export const VERSION = '0.1.0-m2';
